name: saferoute_core
description: SafeRoute shared core package — models, services, utils, constants
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.8.4
  
  # State management
  flutter_riverpod: ^2.6.1
  
  # Storage
  flutter_secure_storage: ^9.2.4
  
  # Utils & Device
  intl: ^0.20.2
  uuid: ^4.5.1
  geolocator: ^13.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
